# 完整 E2E / 富文本真实 API 当前失败关闭，等待后端已提交隔离 runner 接口接入。
# 不填写线上 BACKEND_URL、E2E_BASE_URL、真实测试账号或密码。
# E2E_ENV=test、loopback、专用账号均不能单独证明数据隔离。
# 线上只读烟雾默认访问 https://wenyou.site；也可显式检查已部署 Web 的匿名读取。
SMOKE_BASE_URL=https://wenyou.site
