# 完整 E2E / 富文本真实 API 由已提交后端 runner 创建本轮全新资源。
# 变量需显式 export 给测试命令，脚本不会加载 .env 文件。
# WENYOUSITE_E2E_BACKEND_ROOT=/absolute/path/to/clean/backend-checkout
# WENYOUSITE_E2E_BACKEND_REF=<完整 40 位已校验提交 SHA>
# E2E_PG_BIN=/opt/wenyousite/e2e-tools/usr/lib/postgresql/16/bin
# E2E_REDIS_BIN=/opt/wenyousite/e2e-tools/usr/bin/redis-server
# E2E_LIBRARY_PATH=/opt/wenyousite/e2e-tools/usr/lib/x86_64-linux-gnu
# 不填写线上 BACKEND_URL、E2E_BASE_URL、真实测试账号或密码。
# E2E_ENV=test、loopback、专用账号均不能单独证明数据隔离。
# 线上只读烟雾默认访问 https://wenyou.site；也可显式检查已部署 Web 的匿名读取。
SMOKE_BASE_URL=https://wenyou.site
