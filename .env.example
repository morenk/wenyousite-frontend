# 正式构建/部署服务端后端地址；实时开发请用 dev:preview 的 consumer 描述
# dev:preview 自动设置核验代理地址，不从此变量推断隔离身份
BACKEND_URL=http://127.0.0.1:3000

# 契约脚本可选来源：在 shell 中 export 为已核验 Git SHA 的后端 Worktree 根目录。
# 未设置时继续使用相邻 ../wenyousite-backend；显式指定但缺少契约时检查失败。
# WENYOUSITE_BACKEND_ROOT=/path/to/wenyousite-backend

# WENYOU_PREVIEW_* 由 dev:preview 启动器设置，不手填；只投射非敏感标识/媒体 origin。

# WENYOU_PREVIEW_TASK / WENYOU_PREVIEW_WEB_SESSION 为公开任务标识与独立随机 Web 启动标识。
# 两者均只由启动器生成，不能替代业务认证或复用私有进程控制 token。
