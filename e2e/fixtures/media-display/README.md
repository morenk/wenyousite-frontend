# 完整动画契约素材

来源：Backend `94934be265e36e2f6dba2fbc8b53e58e2755fa52` 的 `contracts/fixtures/media-display/`。GIF包含相邻重复红帧（300ms、600ms）及蓝帧（600ms），两次循环；WebP为实际完整编码器产物，完整时序由后端校验。manifest记录原始SHA-256，浏览器用例验证字节来源与实际像素/有限循环。此受控样本不代表真实用户素材压缩率。
